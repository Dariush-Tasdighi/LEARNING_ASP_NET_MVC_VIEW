<!DOCTYPE html>

<html>
<head>
	<meta name="viewport" content="width=device-width" />

	<title>Learn 1270</title>
</head>
<body>
	@Html.Partial(partialViewName: "_Partial_DisplayCurrentPersianDate")

	@Html.Partial(partialViewName: "_Partial_DisplayCurrentPersianDate")

	@Html.Partial(partialViewName: "_Partial_DisplayCurrentPersianDate")

	@Html.Partial(partialViewName: "_Partial_DisplayCurrentPersianDate")

	@Html.Partial(partialViewName: "_Partial_DisplayCurrentPersianDate")

	@for (int intIndex = 1; intIndex <= 5; intIndex++)
	{
		@Html.Partial(partialViewName: "_Partial_DisplayCurrentPersianDate")
	}
</body>
</html>
